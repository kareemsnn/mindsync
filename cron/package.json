{
  "name": "expiration-scheduler",
  "version": "1.0.0",
  "description": "Cron job for processing expired questions and sending user answers for classification",
  "main": "cron.js",
  "type": "module",
  "scripts": {
    "start": "node cron.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "axios": "^1.6.0",
    "dotenv": "^16.3.1",
    "node-cron": "^3.0.2"
  }
} 